# MiSTercade V1 Freeplay Mappings (No Menu Combo)

V1 No-Menu Freeplay mapping database for the [MiSTercade](https://misteraddons.com/products/mistercade).

This variant has the **menu combo (Down + Start) disabled** so it cannot be accidentally triggered during gameplay. Start also triggers Coin (freeplay).

## How to Integrate into MiSTer Downloader

Add the following to the end of `downloader.ini` on your MiSTer SD card:

```ini
[misteraddons/mistercade_nomenu_freeplay_mappings]
db_url = https://raw.githubusercontent.com/misteraddons/mistercade_nomenu_freeplay_mappings/db/db.json.zip
```

Then run **downloader** or **update_all**.

## All Mapping Variants

See the full list of mapping repositories and variants at: https://github.com/misteraddons/mistercade_mappings
